"""Utilities for acquiring paths that are used often."""

from pathlib import Path


def data_paths() -> Path:
    """
    Utility function that returns paths to the data directories.

    Returns
    -------
    Path
        Return path to the data directories.
    """
    return Path()
